## **מסמך הטמעה moh-package 1.1.0**
## תקציר:

Package 1.1.0 עובר לתמוך בייבוא ותרגום טקסטים מUmbraco Cms, אם כי הוא תומך גם לאחור לפרויקטים ששלחו hard coded text.

עם זאת עדיין נדרשים שלבי הטמעה הכרחיים (לא רשות) לצורך התאמת הpackage שכולל את המנגנון הרוחבי בכל הcomponents, לאפליקציה המשתמשת בתשתית.

**באגים שטופלו:**

1. איפשור עבור formControl )בלבד) לבצע formControl.setValue(null).

    עבור formGroup או formArray יש להשתמש בservice ייעודי שהוספנו.

**היכולות שנוספו:**

1. יכולת bind &amp; translate של תוויות, טקסטים של הודעות שגיאה או הודעות קופצות (modals), כותרות וכו&#39;.
2. יכולת של cascading select, במקרה ששני רכיבי select מבקשים ל&quot;פלטר&quot; את ערכי רכיב select הבן עבור ערך שנבחר ברכיב select האב.
3. נוסף שירות שמאפשר עוד פעולות על ה abstract control הבסיסי שאנחנו עובדים איתו בangular forms. השירות מעתה מאפשר לאפס רכיב AbstractControl למבנה אובייקט המידע שלו עם ערכי null.

## הטמעה:

1. עדכון הגרסה בקובץ package.json

    &quot;moh-package&quot;: &quot;file:P:/npm package/moh-package-1.1.0.tgz&quot;

1. להריץ את הפקודה npm install
2. יש להוסיף בקובץ ה-environment.ts (הקובץ שסביבת הפיתוח משתמשת בו), את המפתח הבא:

```json
umbracoApiURL: "http://umbracodataproviderapi.dev.health.gov.il/"
```

4. בקובץ  app.component.ts

    - יש לייבא את שירות התרגום שנוסף לגירסה זו כך:

      ```typescript
      import { MohTranslateService } from 'moh-package';
      ```

    - וב - Constructor לעשות לו Inject  כך:

      ```typescript
      constructor(private translate: MohTranslateService) { }
      ```

    - בפונקציה ngOnInit יש להוסיף את הקוד הבא:

      ```typescript
      let defaultLanguage: string = 'he';
      this.translate.setDefaultLang(defaultLanguage);
      ```


## תיקוני באגים:

ניתן לעשות setValue עם null ל FormControl. (FormControl בלבד – לא FormGroup או  (FormArray

## שימוש ביכולות:

**Translate**

יכולות חדשות לרכיבים קיימים:

**ניהול ייבוא ותרגום טקסטים על המסך מUmbraco Cms.**

יכולת זו נוספה באופן רוחבי לכל רכיב התשתית הקיימים, רכיבי Input, ורכיבים מורכבים.

הערה: כל הרכיבים תמכו עד עכשיו בטקסט hard coded, ועדיין נתמכים באופן הרגיל. (חלקם ב @Input labelText, חלקם בשדות @Input אחרים ייעודיים לטקסט קבוע)

מעתה הרכיבים הקיימים מאפשרים בנוסף לקבל key של טקסט (label\error message וכו&#39;( להצגת הערך הרלוונטי המנוהל בUmbraco Cms.

**LabelText vs TextKey**

לכל רכיבי הform Controls בתשתית שחשפו עד היום @Input labeltext, יחשפו מעתה בנוסף את ה@Inputs הבאים:
```typescript
@Input() textKey: string

@Input() textParams?: any

@Input() useDefaultKey: boolean = true
```
כאשר ה@Inputs לעיל מיועדים לטפל בהצגת הטקסט לתווית (מתורגמת) מUmbraco.

אופן השימוש בפרמטרים:

**textKey** –מקבל את הקוד שמופיע באומברקו עבור כותרת השדה (labelText),

- אם נשלח LabelText, הערך בInput הזה יוצג, גם אם נשלח textKey.
- אם לא נשלחערך ב labelText ונשלח textKey, תתבצע שליפה של הטקסט מאומברקו על פי ה textKey.
- אם לא נשלחערך ב labelText ולא נשלח ערך בtextKey, הערך בformControlName שהוגדר לformControl ישמש במקרה זה כtextKey.

**textParams** - במקרה שעבור הtextKey, מוגדר באומברקו ביטוי שחלקים בתוכו דינמיים לדוג&#39;

 &quot;שלום {{name}},ברוך הבא לשיעור {{lesson}}&quot;

יש אפשרות לשלוח את האובייקט הבא ל textParams:

{ name:&#39;רבקה&#39;, lesson:&#39;אנגולר&#39; }

ותתבצע &quot;השמה&quot; של ערכי הפרמטרים שנשלחו לתוך הביטוי שהוגדר עבור הtextKey.

**useDefaultKey** –

ערך ברירת המחדל בשדה זה הוא true, ומשמעו שאם לא נשלחערך ב labelText ולא נשלח ערך בtextKey, אז הערך בformControlName שהוגדר לformControl ישמש במקרה זה כtextKey.

אך אם יישלח בשדה זה false, וגם לא יישלחו ערכים ב  labelTextוב textKey, אז גם הformControlName לא ייחשב כtextKey ולא יופיע שום טקסט בlabel.

מסקנה: ע&quot;מ להסתיר את כותרת השדה יש לשלוח useDefaultKey=false, ולא לשלוחערכים ב  labelTextוב textKey.



**דוגמאות:**

- שימוש בlabelText

  ```html
  <moh-textbox labelText="שם פרטי"></moh-textbox> 
  ```

- שימוש ב textKey

  ```html
  <moh-textbox textKey="firstName"></moh-textbox>
  ```

- שימוש ב  textParams

  ```html
  <moh-textbox textKey="title" textParams="{name:'רבקה',lesson:'אנגולר'}">
  </moh-textbox>
  ```

כאשר הטקסט באומברקו הוא  &quot;שלום {{name}},ברוך הבא לשיעור {{lesson}}&quot; והקוד הוא &quot;title&quot;

**Other Text @Inputs vs Other TextKey @Inputs**

לכל הרכיבים שקיבלו עד עכשיו input טקסט התווספו :

עבור כל Input המקבל טקסט התווסף Input מקביל בשם זהה עם הסיומת key, אלא אם כן מפורט אחרת בהמשך.

ה input מקבל מפתח של הטקסט הנמצא באומברקו ,
במקרה שלא נשלח טקסט ל input הקיים - תתבצע שליפה של הטקסט מאומברקו על פי המפתח. (ה input הקיים לטקסט קבוע גובר).

להלן הרכיבים בהם התווספו ה Inputs הנ&quot;ל:

**label-field component**
```typescript
@Input() textKey: string - קוד של הטקסט הקיים באומברקו

@Input() textParams?: any – פרמטרים לטקסט שקיים באומברקו
```
**Textbox component**
```typescript
@Input() placeholderKey: string = ''
```
**select component**
```typescript
@Input() placeholderKey: string = '';
```
**Checkbox component**
```typescript
@Input() textKey: string

@Input() textParams?: any
```
**Datepicker component**
```typescript
@Input() placeholderKey: string;
```
**Button component**
```typescript
@Input() textKey: string

@Input() textParams?: any
```
**submit-button component**
```typescript
@Input() textKey: string

@Input() textParams?: any
```
**tooltip component**
```typescript
@Input() textKey: string

@Input() textParams?: any
```
**Banner component**
```typescript
@Input() mainTextKey: string;

@Input() mainTextParams?: any;

@Input() subTextKey: string;

@Input() subTextParams?: any;
```
**file-upload component**
```typescript
@Input() buttonTextKey: string;

@Input() fieldTextKey: string;

@Input() tooltipTextKey: string;

@Input() tooltipTextParams: any;
```
**form-title component**
```typescript
@Input() titleUnderImageKey: string;

@Input() titleUnderImageParams: string;

@Input() mainImageTextKey: string;

@Input() mainImageTextParams?: any;

@Input() subImageTextKey: string;

@Input() subImageTextParams?: any;
```
**section-title component**
```typescript
@Input() textKey: string // title @Inputברכיב זה השדה הזה מקביל לשדה הקיים

@Input() textParams?: any
```
**info component**
```typescript
@Input() textKey: string

@Input() textParams?: any
```


**Menu/Navigation Components**

**IMenuItem interface (for navbar / dropdown-menu components)**
```typescript
titleKey?: string;
```
**dropdown-menu component**
```typescript
@Input() textKey: string // buttonText @Inputברכיב זה השדה הזה מקביל לשדה הקיים

@Input() textParams?: any
```
**Step class (for wizard-route component)**
```typescript
titleKey?: string;
```


**wizard-route component**
```typescript
@Input() prevButtonTextKey: string = 'prevStep';
@Input() nextButtonTextKey: string = 'nextStep';
```
**Moh-Validators**

**mohValidationErros**
```typescript
errorMessageKey?: string
```
**mohValidator**
```typescript
messageKey?:string  - עבור כל פונקציות הולידציה נוסף הפרמטר
```


**Cascading select**

**Select component**

לרכיב  select  נוספה אופציה של הגדרת תלויות בין שני רכיבים, אב ובן לדוג&#39;: עיר ורשימת רחובות

היכולת הזו ניתנת למימוש כך:

על ה-select  הבן לספק שלושה נתונים:
```typescript
@Input() parentSelect: AbstractControl = null //יש לשלוח את האבא
@Input() parentSortField: string='' //שם השדה באובייקט האבא שישמש לסינון
@Input() childSortField: string = '' //שם השדה באובייקט הבן ParentSortField להשוואה מול-השדה שנשלח ב
```

לדוגמא:

Parent select:
```html
<moh-select [options]="optionsList" placeholderKey="select" displayField="text" [defaultValue]="optionsList[2]" [isAutoComplete]=true formControlName="select" [MarkAsRequired]=true></moh-select>
```
Child select:
```html
<moh-select [options]="optionsChildList" placeholderKey="childSelect" displayField="text" [isAutoComplete]=true formControlName="childSelect" [MarkAsRequired]=true [parentSelect]="demoForm.controls.select" [parentSortField]="'Id'" [childSortField]="'Id_c'"></moh-select>
```
Options list in ts file:
```typescript
optionsList: Observable<any[]> =Observable.of([
    { Id: 1, text: 'רישום יבואן' },
    { Id: 2, text: 'אישור מוקדם ליבוא מזון רגיש ' },
    { Id: 3, text: 'הצהרה' },
    { Id: 4, text: 'בקשה חדשה' }
  ]);
  optionsChildList: Observable<any[]> = Observable.of([
    { Id: 1, Id_c: 1, text: 'ילד של רישום יבואן 1' },
    { Id: 2, Id_c: 1, text: 'ילד של רישום יבואן 2' },
    { Id: 3, Id_c: 2, text: 'ילד של אישור מוקדם 1' },
    { Id: 4, Id_c: 2, text: 'ילד של אישור מוקדם 2' },
    { Id: 5, Id_c: 3, text: 'ילד של הצהרה 1' },
    { Id: 6, Id_c: 4, text: ' 1 ילד של בקשה חדשה' }
  ]);

```
**BaseAbstractControl:**

הוספת שירות  AbstractControlUtilsService עם מתודה setNullValue המאפשרת לאפס כל סוג של abstractControl (FormControl, FormGroup, FormArray)

אופן השימוש:

יש להזריק ב- constructor של ה component שרוצה לאתחל את ה control את AbstractControlUtilsService, ולהפעיל את המתודה כך:

```typescript
this. AbstractControlUtilsService.setNullValue(controlToReset); 
```

**LabelBase class**

נוספה מחלקה חדשה המטפלת בתרגום טקסט.

כל component מותאמת אישית שצריכה טיפול בתרגום טקסט יכולה לרשת מהמחלקה LabelBase, ולקבל בירושה את המאפיינים והפונקציות הבאות:

Properties
```typescript
@Input() textKey: string- קוד של הטקסט הקיים באומברקו

@Input() textParams?: any – פרמטרים לטקסט שקיים באומברקו
```
```typescript
get textValue(): Observable<string>- 
```
 מחזיר את הטקסט המתורגם עבור הקוד ב @Input textKey
בשילוב הפרמטרים ב @Input textParams


Methods
```typescript
getLabelText(textKey, textParams?:any): Observable<string>
```
מחזירה את הטקסט המתורגם של המפתח או את המפתח אם הוא לא נמצא.
```typescript
getInstantLabelText(textKey, textParams?: any): string
```
מחזירה את הטקסט המתורגם המיידי של המפתח.

**אופן השימוש במאפיינים ובפונקציות:**

- לדוגמה במקרה שהcomponent צריך תרגום או ייבוא מUmbraco Cms של טקסט:

קוד html עם טקסט hard coded:

```html
<div>
  <span>דוגמה לתרגום</span>
  <span>דוגמה לתרגום עם פרמטר {{param}}</span>
</div>
```

על מנת לייבא טקסט מ Umbraco Cms יש לממש בצורה הבאה:

```html
<div>
  <span>{{getLabelText(‘translateExampleKey’) | async}}</span>
  <span>{{getLabelText(‘transWithParamsKey’,{param1:param} )| async}}</span>
</div>
```

- במקרה שהרכיב מקבל @Input טקסט ניתן להשתמש במאפיינים באופן הבא:

קוד html עם @Input טקסט:

```html
<span>{{text}}</span>
```

כאשר  @Input() text?: string

על מנת לקבל @input key של הטקסט ולייבא את הטקסט מ Umbraco Cms יש לממש בצורה הבאה:

```html
<span>{{textValue | async}}</span>
```

באופן כזה המשתמש ברכיב ישלח את ה key ב @input textKey   שהתקבל בירושה ממחלקת הבסיס LabeBase, וישלח את הפרמטרים לטקסט ב @Input textParams שאף הוא התקבל בירושה,

כתוצאה מכך המאפיין textValue שהתקבל בירושה יחזיר את הטקסט המתאים ל textKey שנשלח ולכן יש להציג אותו ב html.
