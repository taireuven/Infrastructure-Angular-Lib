import { MatTableHasAccordionDirective } from './is-accordion.directive';

describe('MatTableHasAccordionDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTableHasAccordionDirective();
    expect(directive).toBeTruthy();
  });
});
