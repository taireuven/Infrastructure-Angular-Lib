import { FocusWizardFormDirective } from './focus-wizard-form.directive';

describe('FocusWizardFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusWizardFormDirective();
    expect(directive).toBeTruthy();
  });
});
